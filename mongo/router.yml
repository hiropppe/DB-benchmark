version: '3'
services:
    mongos:
        image: mongo:4.0.0-xenial
        container_name: mongos
        command: ["mongos", "-f", "/etc/mongos.conf"]
        ports:
            - "27017:27017"
        volumes:
            - ./etc/mongos.conf:/etc/mongos.conf
    mongoc:
        image: mongo:4.0.0-xenial
        container_name: mongoc
        command: ["mongod", "-f", "/etc/mongoc.conf"]
        ports:
            - "27019:27019"
        volumes:
            - ./etc/mongoc.conf:/etc/mongoc.conf
